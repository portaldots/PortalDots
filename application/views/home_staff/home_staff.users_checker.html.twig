{% extends "layout_main.html.twig" %}

{% block title %}
  ユーザー登録チェッカー - {{ constant("APP_NAME") }}
{% endblock %}

{% block maincontent %}

  <h1 class="main__title">ユーザー登録チェッカー</h1>

  {{ form_open() | raw }}
    <input class="form-control input-lg" name="student_id" placeholder="学籍番号" autofocus>
    <button class="btn btn-primary btn-lg" type="submit">確認</button>
  {{ form_close() | raw }}

  {% if user_check is not empty %}
    {% if user_check.is_completed_register == true %}
      <h2 style="font-size: 6rem;" class="text-success">{{ user_check.student_id }}</h2>
      <p><b>登録されています</b></p>
    {% else %}
      <h2 style="font-size: 6rem;" class="text-warning">{{ user_check.student_id }}</h2>
      <p class="text-danger"><b>まだ登録が完了していません。確認メール送信後、24時間経過していない場合、開いていない確認メールがないか、もう一度ご確認ください。24時間経過してしまったか、確認メールが見つからない場合、恐れ入りますが、もう一度ユーザー登録を、最初からやり直してください。</b></p>
    {% endif %}

    <ul class="list-unstyled">
      <li style="font-size: 4rem; font-weight: bold">{{ user_check.name_family }} {{ user_check.name_given }}</li>
      <li style="font-size: 2.5rem; font-weight: bold">( {{ user_check.name_family_yomi }} {{ user_check.name_given_yomi }} )</li>
    </ul>
  {% else %}
    <h2 class="text-danger">未登録</h2>
  {% endif %}

{% endblock %}
